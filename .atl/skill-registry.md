# Skill Registry — graficos

<!-- Auto-generated by gentle-ai skill-registry refresh. Run `gentle-ai skill-registry refresh --force` to regenerate. -->

Last updated: 2026-05-21

## Sources scanned

- C:\Users\gabri\.pi\agent\skills
- C:\Users\gabri\.config\opencode\skills
- C:\Users\gabri\.claude\skills
- C:\Users\gabri\.gemini\skills
- C:\Users\gabri\.gemini\antigravity\skills
- C:\Users\gabri\.copilot\skills

## Contract

**Delegator use only.** Any agent that launches subagents reads this registry to resolve compact rules, then injects matching rule text into subagent prompts under `## Project Standards (auto-resolved)`.

Subagents still read their assigned executor/phase skill. During normal runtime, they do **not** independently discover or load additional project/user `SKILL.md` files or this registry; project/user rules arrive pre-digested. Explicit fallback loading is degraded self-healing and must be reported in `skill_resolution` as `fallback-registry` or `fallback-path`.

## Selected skills and compact rules

### accounting-analyst
- Path: C:\Users\gabri\.config\opencode\skills\sdd-experts\accounting-analyst\SKILL.md
- Trigger: >
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### branch-pr
- Path: C:\Users\gabri\.config\opencode\skills\branch-pr\SKILL.md
- Trigger: Create Gentle AI pull requests with issue-first checks. Trigger: creating, opening, or preparing PRs for review.
- Rules:
  - **Every PR MUST link an approved issue** — no exceptions
  - **Every PR MUST have exactly one `type:*` label**
  - **Automated checks must pass** before merge is possible
  - **Blank PRs without issue linkage will be blocked** by GitHub Actions

### brand-guidelines
- Path: C:\Users\gabri\.config\opencode\skills\brand-guidelines\SKILL.md
- Trigger: Trigger: brand, branding, corporate identity, brand colors, company colors, style guide. Apply official brand colors, typography, and visual identity to any artifact — presentations, documents, reports, or HTML.
- Rules:
  - **Always ask for company/brand name** if not specified. Research official brand colors.
  - **Use exact hex values** — no approximations.
  - **Apply to ALL elements**: headers, body, shapes, accents, backgrounds.

### brave-search
- Path: C:\Users\gabri\.pi\agent\skills\pi-skills\brave-search\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### chained-pr
- Path: C:\Users\gabri\.config\opencode\skills\chained-pr\SKILL.md
- Trigger: Trigger: PRs over 400 lines, stacked PRs, review slices. Split oversized changes into chained PRs that protect review focus.
- Rules:
  - Split PRs over **400 changed lines** unless a maintainer explicitly accepts `size:exception`.
  - Keep each PR reviewable in about **≤60 minutes**.
  - Use one deliverable work unit per PR; keep tests/docs with the unit they verify.
  - State start, end, prior dependencies, follow-up work, and out-of-scope items in every chained PR.
  - Every child PR must include a dependency diagram marking the current PR with `📍`.
  - In Feature Branch Chain, create a draft/no-merge tracker PR; child PR #1 targets the tracker branch, later children target the immediate parent branch.
  - Treat polluted diffs as base bugs: retarget or rebase until only the current work unit appears.
  - Do not mix chain strategies after the user chooses one.
  - Condition: Action
  - PR ≤400 changed lines and focused: Keep single PR.
  - PR >400, each slice can land independently: Use Stacked PRs to main.
  - PR >400, feature must integrate before main: Use Feature Branch Chain with tracker.
  - Generated/vendor/migration diff cannot split cleanly: Ask maintainer for `size:exception`.
  - SDD provides `delivery_strategy`: Follow it before apply/PR creation.

### clasificacion-contable
- Path: C:\Users\gabri\.pi\agent\skills\clasificacion-contable\SKILL.md
- Trigger: Clasifica transacciones de extractos bancarios contra plan de cuentas (ledger), account list y vendor list usando matching difuso. Workflow completo con sistema de handoff via task.md. Activar cuando mencionen clasificar extractos, conciliar, procesar movimientos bancarios.
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### cognitive-doc-design
- Path: C:\Users\gabri\.config\opencode\skills\cognitive-doc-design\SKILL.md
- Trigger: Design docs that reduce cognitive load. Trigger: writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs.
- Rules:
  - Pattern: Rule
  - Lead with the answer: Put the decision, action, or outcome first. Context comes after.
  - Progressive disclosure: Start with the happy path, then add details, edge cases, and references.
  - Chunking: Group related information into small sections. Keep flat lists short.
  - Signposting: Use headings, labels, callouts, and summaries so readers know where they are.
  - Recognition over recall: Prefer tables, checklists, examples, and templates over prose that must be remembered.
  - Review empathy: Design docs so reviewers can verify intent without reconstructing the whole story.

### comment-writer
- Path: C:\Users\gabri\.config\opencode\skills\comment-writer\SKILL.md
- Trigger: Write warm, direct collaboration comments. Trigger: PR feedback, issue replies, reviews, Slack messages, or GitHub comments.
- Rules:
  - Be useful fast: Start with the actionable point. Do not recap the whole PR before feedback.
  - Be warm and direct: Sound like a thoughtful teammate, not a corporate bot.
  - Keep it short: Prefer 1 to 3 short paragraphs or a tight bullet list.
  - Explain why: Give the technical reason when asking for a change.
  - Avoid pile-ons: Comment on the highest-value issue, not every tiny preference.
  - Match thread language: Write in the thread/user language. If writing in Spanish, use Rioplatense Spanish/voseo: `podés`, `tenés`, `fijate`, `dale`.
  - No em dashes: Use commas, periods, or parentheses instead.

### dash
- Path: C:\Users\gabri\.config\opencode\skills\dash\SKILL.md
- Trigger: Trigger: Dash app, dashboard, data app, Dash Plotly, interactive dashboard, web dashboard, real-time dashboard. Build interactive data applications and dashboards with Dash Python framework. Covers layout, callbacks, Plotly integration, Bootstrap theming, multi-page, and deployment.
- Rules:
  - **Core pattern**: `layout` defines UI, `@callback` links inputs to outputs, `app.run_server(debug=True)` to launch.
  - **Dependencies**: `pip install dash plotly pandas dash-bootstrap-components dash-ag-grid`
  - **Default Bootstrap theme**: Use `dbc.themes.CERULEAN` or `dbc.themes.BOOTSTRAP` for professional UI.
  - **Single-page** by default. Multi-page for >3 distinct views.
  - **Output to**: Save as `app.py` in `outputs/<dashboard-name>/`.
  - Need: Approach
  - Simple dashboard: Single `app.py` with `dcc.Graph` + callbacks
  - Professional UI: Use `dash-bootstrap-components` (`dbc`)
  - Data tables: `dash-ag-grid` for interactive grids
  - Multi-page: `dcc.Location` + URL callbacks
  - Real-time updates: `dcc.Interval` for periodic refreshes
  - Authentication: Dash Enterprise or custom auth
  - Server deployment: Gunicorn + Waitress (production)

### data-analyst
- Path: C:\Users\gabri\.config\opencode\skills\sdd-experts\data-analyst\SKILL.md
- Trigger: >
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### docx
- Path: C:\Users\gabri\.pi\agent\skills\skills\skills\docx\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### financial-analyst
- Path: C:\Users\gabri\.config\opencode\skills\sdd-experts\financial-analyst\SKILL.md
- Trigger: >
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### go-testing
- Path: C:\Users\gabri\.config\opencode\skills\go-testing\SKILL.md
- Trigger: Trigger: Go tests, go test coverage, Bubbletea teatest, golden files. Apply focused Go testing patterns.
- Rules:
  - Prefer table-driven tests for multiple cases; use `t.Run(tt.name, ...)`.
  - Test behavior and state transitions, not implementation trivia.
  - Use `t.TempDir()` for filesystem tests; never rely on a real home directory.
  - Keep integration tests skippable with `testing.Short()` when they run external commands or slow flows.
  - For Bubbletea, test `Model.Update()` directly for state changes; use `teatest` only for interactive flows.
  - Golden files must be deterministic; update only through the repo's `-update` path and rerun tests without `-update`.
  - Use small mocks/interfaces around system or command execution boundaries.
  - Pure function or parser: Table-driven unit test.
  - Error behavior: Explicit success and failure cases.
  - File operations: `t.TempDir()` plus focused assertions.
  - TUI state transition: Direct `Model.Update()` call with `tea.Msg`.
  - Full TUI interaction: `teatest.NewTestModel()`.
  - Rendered output: Golden file test.
  - Real external command: Integration test; skip in `-short`.

### infrastructure-expert
- Path: C:\Users\gabri\.config\opencode\skills\sdd-experts\infrastructure-expert\SKILL.md
- Trigger: >
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### issue-creation
- Path: C:\Users\gabri\.config\opencode\skills\issue-creation\SKILL.md
- Trigger: Create Gentle AI issues with issue-first checks. Trigger: creating GitHub issues, bug reports, or feature requests.
- Rules:
  - **Blank issues are disabled** — MUST use a template (bug report or feature request)
  - **Every issue gets `status:needs-review` automatically** on creation
  - **A maintainer MUST add `status:approved`** before any PR can be opened
  - **Questions go to [Discussions](https://github.com/Gentleman-Programming/agent-teams-lite/discussions)**, not issues

### judgment-day
- Path: C:\Users\gabri\.config\opencode\skills\judgment-day\SKILL.md
- Trigger: Trigger: judgment day, dual review, adversarial review, juzgar. Run blind dual review, fix confirmed issues, then re-judge.
- Rules:
  - Resolve project skills before launching agents: read skill registry, match compact rules by target files/task, and inject the same `Project Standards` block into both judge prompts and fix prompts.
  - Launch **two blind judges in parallel** with identical target and criteria; never review the code yourself.
  - Wait for both judges before synthesis; never accept a partial verdict.
  - Classify warnings as `WARNING (real)` only if normal intended use can trigger them; otherwise downgrade to INFO as `WARNING (theoretical)`.
  - Ask before fixing Round 1 confirmed issues.
  - After any fix agent runs, immediately re-launch both judges in parallel before commit/push/done/session summary.
  - Terminal states are only `JUDGMENT: APPROVED` or `JUDGMENT: ESCALATED`.
  - After 2 fix iterations with remaining issues, ask the user whether to continue.
  - Condition: Action
  - Target unclear: Ask for scope; do not launch judges.
  - No skill registry: Warn, proceed with generic criteria, and record `Skill Resolution: none`.
  - Both judges find same CRITICAL/real WARNING: Confirmed; ask/fix according to round rules.
  - One judge finds issue: Suspect; report and triage, do not auto-fix.
  - Judges contradict: Escalate for manual decision.
  - Round 2+ has only theoretical warnings/suggestions: Report as INFO; do not re-judge.

### office
- Path: C:\Users\gabri\.config\opencode\skills\office\SKILL.md
- Trigger: Trigger: Office document, PPTX, DOCX, XLSX, PDF, PowerPoint, Word, Excel, create presentation, edit document, spreadsheet, form. Master orchestrator that detects format and delegates to the appropriate Python-based Office skill.
- Rules:
  - **Python-only**: All workflows use **python-pptx**, **python-docx**, **openpyxl**, **pypdf**, **pdfplumber**, **reportlab**. No JS dependencies.
  - **Dependencies**: `pip install python-pptx python-docx openpyxl pypdf pdfplumber reportlab Pillow`
  - **System tools** (optional): LibreOffice (`soffice`), Poppler (`pdftoppm`), Pandoc.
  - **Output convention**: `outputs/<document-name>/`
  - User says: Load skill
  - Presentation, PowerPoint, PPTX, slides, deck: `office-pptx`
  - Word, DOCX, document, report, letter, redline: `office-docx`
  - Excel, XLSX, spreadsheet, financial model: `office-xlsx`
  - PDF, form, fill, merge, extract: `office-pdf`

### office-docx
- Path: C:\Users\gabri\.config\opencode\skills\office-docx\SKILL.md
- Trigger: Trigger: Word, DOCX, document, report, letter, memo, redline, tracked changes. Create, edit, and analyze Word documents using python-docx. Supports tracked changes and comments via OOXML editing.
- Rules:
  - **python-docx** for creation. OOXML for tracked changes.
  - **Use smart quotes** in new content: `&#x201C;` `&#x201D;` `&#x2019;`.
  - **RSIDs must be 8-digit hex**: e.g. `00AB1234`.
  - **Use proper list numbering** — never unicode bullets.
  - **Output to**: `outputs/<document-name>/final.docx`
  - Need: Action
  - Read text: `pandoc --track-changes=all file.docx -o output.md`
  - Create new: python-docx native API
  - Tracked changes: Unpack → edit `word/document.xml` → repack
  - Add comments: Unpack → run comment helper → edit XML markers
  - Accept all changes: LibreOffice macro or OOXML script

### office-pdf
- Path: C:\Users\gabri\.config\opencode\skills\office-pdf\SKILL.md
- Trigger: Trigger: PDF, form, fill, merge, split, extract, convert. Manipulate PDF files using pypdf, pdfplumber, and reportlab. Handle form filling, merging, extraction, and creation.
- Rules:
  - **pypdf** for basic operations (merge, split, encrypt, form fill).
  - **pdfplumber** for text/table extraction.
  - **reportlab** for creating new PDFs (canvas + platypus).
  - **System tools** (optional): `pdftoppm` for PDF→images, `pytesseract` for OCR.
  - **Output to**: `outputs/<project-name>/final.pdf`
  - Need: Action
  - Read/extract text: pdfplumber `extract_text()`
  - Extract tables: pdfplumber `extract_tables()`
  - Merge/split: pypdf PdfReader + PdfWriter
  - Create new PDF: reportlab (canvas or platypus)
  - Fill interactive form: pypdf `update_page_form_field_values()`
  - Fill non-interactive PDF: Add text annotations via pypdf
  - Encrypt/decrypt: pypdf `writer.encrypt()` / `reader.decrypt()`
  - Convert to images: `pdftoppm -jpeg -r 150 input.pdf output`
  - OCR scanned PDF: pdf2image + pytesseract

### office-pptx
- Path: C:\Users\gabri\.config\opencode\skills\office-pptx\SKILL.md
- Trigger: Trigger: PowerPoint, PPTX, presentation, slides, deck. Create, edit, and analyze PowerPoint presentations using python-pptx. Includes design, visual QA, and template workflows — all Python, no JS or browser dependencies.
- Rules:
  - **Design before code**: State your color palette, typography pairings, and layout approach BEFORE writing code.
  - **python-pptx accepts `#RRGGBB`** for colors (unlike PptxGenJS which forbids the `#`).
  - **Output to**: `outputs/<deck-name>/final.pptx`
  - **QA is mandatory**: Assume your first render has issues. Fix → re-verify → repeat.
  - Need: Action
  - Read text: `python -m markitdown file.pptx`
  - Create from scratch: python-pptx (see reference)
  - Create from template: Template workflow (iterate slides → replace text)
  - Visual QA: PPTX → PDF → images → subagent inspection
  - OOXML deep edit: Read `references/ooxml-guide.md` from office skill

### office-xlsx
- Path: C:\Users\gabri\.config\opencode\skills\office-xlsx\SKILL.md
- Trigger: Trigger: Excel, XLSX, spreadsheet, financial model, data analysis, workbook. Create and edit Excel spreadsheets with formulas, formatting, and charts using openpyxl and pandas.
- Rules:
  - **Zero formula errors**: Every model MUST deliver ZERO errors (#REF!, #DIV/0!, #VALUE!, #N/A, #NAME?).
  - **Use formulas, not hardcoded values**: Excel should be dynamic.
  - **Color coding for financial models**:
  - Blue (`0000FF`): Hardcoded inputs
  - Black (`000000`): Formulas
  - Green (`008000`): Cross-sheet references
  - Yellow fill (`FFFF00`): Key assumptions
  - **Number formatting**: Years as text, zeros as `-`, parentheses for negatives.
  - **Output to**: `outputs/<model-name>/final.xlsx`
  - Need: Action
  - Read/analyze data: pandas `read_excel()`
  - Create with formulas + formatting: openpyxl
  - Create simple data export: pandas `to_excel()`
  - Edit existing preserving formulas: openpyxl `load_workbook()`
  - Recalculate formulas: LibreOffice headless recalculation

### open-websearch
- Path: C:\Users\gabri\.pi\agent\skills\open-websearch\SKILL.md
- Trigger: Web search and content extraction via the open-websearch CLI (npm package open-websearch). Use for searching the web, fetching page content as markdown, fetching GitHub READMEs, and extracting content from Chinese platforms (CSDN, Juejin, Linux.do). Fast, no browser required for basic searches.
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### pdf
- Path: C:\Users\gabri\.pi\agent\skills\skills\skills\pdf\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### playwright-mcp
- Path: C:\Users\gabri\.config\opencode\skills\playwright-mcp\SKILL.md
- Trigger: >
- Rules:
  - Actual rendered HTML (React produces different markup than source)
  - Whether elements are visible/hidden
  - Correct `role` attributes (Radix components differ from HTML defaults)
  - Which elements are actually interactive
  - Priority: Selector type
  - 1: `getByText` (exact)
  - 2: `getByRole` + name
  - 3: `getByLabel`
  - 4: `getByPlaceholder`
  - 5: CSS + ref from snapshot

### playwright-migration
- Path: C:\Users\gabri\.config\opencode\skills\playwright-migration\SKILL.md
- Trigger: >
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### plotly
- Path: C:\Users\gabri\.config\opencode\skills\plotly\SKILL.md
- Trigger: Trigger: chart, plot, graph, visualization, Plotly, interactive chart, data viz, dashboard, 3D plot, heatmap, scatter plot. Create interactive scientific and analytical charts with Plotly Python. Use for data-driven reporting, dashboards, and exports to HTML/PNG/PDF.
- Rules:
  - **Plotly Express** (`px`) for quick, clean charts. **Plotly Graph Objects** (`go`) for complex customization.
  - **Pandas integration first**: always load data with pandas, then plot with plotly.
  - **Export as HTML** for interactive reports: `fig.write_html('file.html')`
  - **Export as static image** for PDF/PPTX: `fig.write_image('chart.png')` (requires `kaleido`)
  - **Use WebGL** (`Scattergl`, etc.) for datasets > 10k points.
  - **Dependencies**: `pip install plotly pandas kaleido`
  - Need: Approach
  - Quick exploratory plot: `plotly.express` (one-liner)
  - Complex multi-trace chart: `plotly.graph_objects`
  - Large data (>10k points): `Scattergl`, `Heatmapgl` for performance
  - Subplots / multi-axis: `make_subplots` + `secondary_y`
  - 3D visualization: `px.scatter_3d`, `go.Surface`
  - Dashboard: Plotly Dash
  - Export for presentation: `write_html()` or `write_image()`

### pptx
- Path: C:\Users\gabri\.pi\agent\skills\skills\skills\pptx\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### security-auditor
- Path: C:\Users\gabri\.config\opencode\skills\sdd-experts\security-auditor\SKILL.md
- Trigger: >
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### skill-creator
- Path: C:\Users\gabri\.config\opencode\skills\skill-creator\SKILL.md
- Trigger: Trigger: new skills, agent instructions, documenting AI usage patterns. Create LLM-first skills with valid frontmatter.
- Rules:
  - When working in this repo, first follow `docs/skill-style-guide.md` as the normative source before creating or updating skills.
  - If that guide is unavailable, use the compact inline rules below.
  - A skill is a runtime instruction contract for an LLM, not human documentation.
  - Do not add a `Keywords` section; preserve essential trigger words in `description`.
  - References must point to local files.
  - Keep the skill body concise: target 180–450 tokens, recommended max 700, hard max 1000.
  - Need: Action
  - Code templates, schemas, fixtures, generated examples: Put them in `assets/`
  - Conceptual detail, edge cases, existing docs: Put local links in `references/`
  - Long explanation in `SKILL.md`: Move it to a supporting file
  - Multiple meaningful paths: Add a compact decision table

### statement-extractor
- Path: C:\Users\gabri\.pi\agent\skills\statement-extractor\SKILL.md
- Trigger: Extract monthly amounts from credit card/bank statement PDFs into CSV using rule-based regex matching. Use when you need recurring spend tracking (e.g., subscriptions by merchant) or monthly interest extraction from statement files like onlineStatement*.pdf.
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### theme-factory
- Path: C:\Users\gabri\.config\opencode\skills\theme-factory\SKILL.md
- Trigger: Trigger: theme, color palette, font pairing, design theme, presentation theme, slide theme. Apply or create professional color/font themes for presentations, documents, and other artifacts. 10 pre-set themes available plus custom theme generation.
- Rules:
  - **Show available themes first** before applying.
  - **Apply ALL colors and fonts** from the chosen theme.
  - **Ensure contrast**: text must be readable on backgrounds.

### ux-researcher
- Path: C:\Users\gabri\.config\opencode\skills\sdd-experts\ux-researcher\SKILL.md
- Trigger: >
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.

### work-unit-commits
- Path: C:\Users\gabri\.config\opencode\skills\work-unit-commits\SKILL.md
- Trigger: Plan commits as reviewable work units. Trigger: implementation, commit splitting, chained PRs, or keeping tests and docs with code.
- Rules:
  - Commit by work unit: A commit represents a deliverable behavior, fix, migration, or docs unit.
  - Do not commit by file type: Avoid `models`, then `services`, then `tests` if none works alone.
  - Keep tests with code: Tests belong in the same commit as the behavior they verify.
  - Keep docs with the user-visible change: Docs belong with the feature or workflow they explain.
  - Tell a story: A reviewer should understand why each commit exists from its diff and message.
  - Future PR-ready: Each commit should be a candidate chained PR when the change grows.
  - SDD workload guard: If SDD tasks forecast a >400-line change, group commits into chained PR slices before implementation.

### xlsx
- Path: C:\Users\gabri\.pi\agent\skills\skills\skills\xlsx\SKILL.md
- Rules:
  - No compact rules declared; delegators should load the full skill file before direct work, or pass an explicit fallback path only when Project Standards cannot be injected.
